using TrenchCats.Events;

namespace Sakura.Geoscape.Events;

public class DialogueEvent : IScheduleActivity
{
    public DialogueEvent(DateTime occurrenceTime, IEventBus eventBus)
    {
        
    }
}